{
  "name": "mellowtech",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "vercel-build": "npm run build"  // Critical for Vercel
  },
  "dependencies": {
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.3.3",          // Required if using TS
    "@types/react": "^18.2.56",      // Required if using TS
    "@types/node": "^20.11.19"       // Required if using TS
  },
  "engines": {
    "node": ">=18.0.0"              // Specifies Node.js version
  }
}